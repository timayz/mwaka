name: "Release"

on:
  push:
    branches:
      - main

jobs:
  node:
    uses: timada-org/.github/.github/workflows/node-release.yml@chore/use-npm
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
