{
  "name": "components",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "rollup -c",
    "build:dev": "pnpm build && rm -rf ../../dist/components && mkdir -p ../../dist && mv dist ../../dist/components"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "mwaka": "workspace:^",
    "rollup": "^2.70.1",
    "rollup-preset-solid": "^1.4.0",
    "solid-js": "^1.3.14",
    "typescript": "^4.6.3"
  }
}
